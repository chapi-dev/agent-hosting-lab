# Self-hosted runtime dependencies.
#
# Note how much of this list is not about agents. The web server, the ASGI
# runner, the telemetry exporter and the database driver are all infrastructure
# that the hosted model supplies. See src/hosted/requirements.txt for the
# equivalent list under Foundry hosting.
agent-framework==1.14.0
agent-framework-foundry==1.11.0
azure-identity==1.25.3
azure-cosmos==4.16.3
azure-monitor-opentelemetry==1.8.9
fastapi==0.138.0
uvicorn[standard]==0.52.3
pydantic==2.12.5
httpx==0.28.1
